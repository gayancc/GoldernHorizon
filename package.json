{
  "name": "golden-horizon",
  "private": true,
  "version": "1.0.0",
  "description": "Golden Horizon — Toronto-based musical group portfolio & landing page",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:functions": "npm test --prefix functions",
    "emulators": "npx firebase-tools emulators:start --only functions,hosting"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "particles.js": "^2.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
